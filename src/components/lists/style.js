import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  textInputStyles: {
    height: 40,
    borderColor: 'gray',
    borderWidth: 1,
  },
  listStyles: {
    flex: 1,
  }
})

export default styles
