import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  textInput: {
    height: 40,
    borderColor: 'gray',
    borderWidth: 1,
  },
  list: {
    flex: 1,
  },
})

export default styles
