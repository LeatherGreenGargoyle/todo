import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  header: {
    fontSize: 30,
    padding: 20,
  },
  task: {
    fontSize: 20,
    marginBottom: 5,
    marginLeft: 30,
  },
})

export default styles
